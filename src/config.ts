// src/config.ts

// AV Technical Support phone number — real number pending from the service
// owner. Leave null until provided; phone links are hidden while null.
export const SUPPORT_PHONE: string | null = null; // tel: format, e.g. "+13105551234"
export const SUPPORT_PHONE_DISPLAY: string | null = null; // display format, e.g. "(310) 555-1234"
